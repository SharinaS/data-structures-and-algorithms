package code401Challenges.hashtable;


import code401Challenges.tree.Tree;

public class TreeIntersection {
    // Traverse through tree1. Add all values to a hashmap with null value. Then, inorder traverse through tree2.
    // If node.value of tree2 in hashmap, add node.value to a second hashmap with null value. Return that second
    // hashmap, since that will only contain values that intersect.


    // output will be a hashmap
    public Hashtable treeOnePutIntoHashtable(Tree tree1) {
        Hashtable hashtable = new Hashtable();
        Tree tree = new Tree();
        return hashtable;
    }

}
